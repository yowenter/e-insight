version: "2.2"
services:
#    prometheus:
#        image: prom/prometheus
#        ports:
#            - 9090:9090
#        volumes:
#            - ./prometheus.yml:/etc/prometheus/prometheus.yml
#            - ./prometheus-data:/prometheus-data

#    grafana:
#        image: grafana/grafana
#        ports:
#           - 3000:3000
#        volumes:
#           - ./grafana-storage:/var/lib/grafana

    insight:
        # docker build -t insight .
        build: .
        # image: insight
        environment:
          DOWNLOADER_PROXY: "http://10.114.0.2:3101/fetch"

        ports:
            - 5500:8000
    
        
        
  